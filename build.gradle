plugins {
    id 'java'
}

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    // «ависимость дл€ JUnit Jupiter
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.10.0'

    // «ависимость дл€ JUnit Jupiter Engine (нужна дл€ запуска тестов)
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.10.0'

    // «ависимость дл€ JUnit Jupiter Params (дл€ использовани€ параметризованных тестов)
    testImplementation 'org.junit.jupiter:junit-jupiter-params:5.10.0'

}

test {
    useJUnitPlatform()
}